# Nuxly Account List Cumulative Balance

Odoo 19 module adding a live "Cumulative Balance" column on `account.move.line` list
views, to help accountants reconcile entries manually (select lines and see whether
they sum to zero).
